services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    user: "node"
    working_dir: /home/node/app
    volumes:
      - ./:/home/node/app
      - node_modules:/home/node/app/node_modules
    ports:
      - "127.0.0.1:3000:3000"

volumes:
  node_modules:
